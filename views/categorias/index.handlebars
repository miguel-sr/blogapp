<div class="card">
  <div class="card-body">
    <h4>Categorias: </h4>
    <ul>
      {{#each categorias}}
        <a class="link-dark d-block my-3" style="text-decoration: none;" href="/categorias/{{slug}}">
          <li>{{nome}}</li>
        </a>
      {{else}}

      {{/each}}
    </ul>
  </div>
</div>